# S03 – Questions / Concepts I Don’t Understand Yet

1) How the browser reads an HTML file from top to bottom and decides what to render first.
2) Why some tags are self-closing (like `<meta />`) and others are not.
3) What `lang="en"` actually does on the `<html>` tag and why it matters.
4) How CSS variables are inherited and where they can and cannot be used.
5) The difference between `display: flex` and `display: grid`, and how to choose between them.
6) What `z-index` controls and why it sometimes seems to not work.
7) How `gap` works differently in flexbox vs grid layouts.
8) What `transition` properties do and how timing functions affect animations.
9) How hover states work on touch devices that don’t have a mouse.
10) Why modern websites use semantic tags like `<header>`, `<main>`, `<section>`, and `<footer>` instead of only `<div>`s.
